<?php
declare(strict_types=1);

/**
 * Разделы страниц: код => название для админки.
 *
 * Раздел — это категория страницы (`pages.section`). По нему страницы
 * группируются в списке и фильтруются в выпадающих списках при выборе
 * ссылки: блок «Карточки» с разделом «Отрасли» предложит только отрасли.
 *
 * Новый раздел заводится одной строкой здесь: справочник читают и список
 * страниц, и форма страницы, и поля-ссылки.
 */
return [
    ''         => 'Основные',
    'industry' => 'Отрасли',
    'model'    => 'Модели',
];
